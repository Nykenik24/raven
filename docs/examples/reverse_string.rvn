function reverseString(str: string) string {
  let reversed: string;
  for (i in str.len..0) {
    reversed += str[i];
  }
  return reversed;
}

function main() int {
  print(reverseString("Hello, World!")) // !dlroW ,olleH
  return 0;
}
