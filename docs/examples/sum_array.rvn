function sumArray(array: [int]) int {
  let sum = 0;
  for (_, value in array) {
    sum += value;
  }
  return sum;
}

function main() int {
  const array = new [int](1, 5, 8, 19, 14, 32);
  print(sumArray(array)); // 79
  return 0;
}
